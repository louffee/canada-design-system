import { create } from '@storybook/theming/create'

const theme = create({
  base: 'light',
  brandTitle: 'Louffee design system',
  brandImage: 'https://github.com/louffee.png?width=48&height=48',
})

export default theme
