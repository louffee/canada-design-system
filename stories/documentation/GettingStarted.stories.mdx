import { Meta } from '@storybook/addon-docs'

import CopyrightFooter from '../_internal/CopyrightFooter'

# Getting started

<Meta title='Documentation/Getting started' />

### For engineers 🛠

**Clone the repository** from our GitHub organisation.

```bash
git clone git@github.com:louffee/canada-design-system.git && cd canada-design-system
```

**Install** the dependencies. ⚠️ **Note that** we use [Yarn](https://yarnpkg.com) as our package manager.

```bash
yarn install && yarn run bootstrap
```

**Run** this [storybook](https://storybook.js.org).

```bash
yarn run storybook
```

<br />
<br />
<br />

### For designers 🎨

**Note that** we use [Figma](https://www.figma.com) as our design tool.

We are **still** creating the basis of the design system. We will update this page as soon as we have something to show.

<CopyrightFooter />
